# `npm version` writes the bare version as the commit subject, which the
# commit-msg hook rejects -- correctly, since the convention applies to every
# commit including npm's own. %s is the new version.
message = "chore(release): %s"
